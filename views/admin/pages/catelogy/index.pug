extends ../../layout/default.pug
include ../../mixins/filter-status.pug
include ../../mixins/search-product.pug
include ../../mixins/pagination-page.pug
include ../../mixins/multi-change-status.pug
include ../../mixins/flash-messages.pug
include ../../mixins/sort.pug
include ../../mixins/category-index.pug
block main
  h1 danh sach san pham
  .card.mb-3
    .card-header Sắp sếp
    .card-body
      .row
        .col-4
        //-   +sort()
        .col-6.d-flex.align-items-center
    .card-header bo loc va tiem kiem
    .card-body
      .row
        .col-6.d-flex.align-items-center
        //-   +filter-status(status)
        .col-6.d-flex.align-items-center  
        //-   +form-search(searchs)
    .card.mb-3
      .card-header Danh sach
        +flashMessage()
        .card-body
          .row
            .col-4
              a(href=`${systemAdmin}/catelogy/create` class=`btn btn-sm ml-1 btn-outline-success`) Thêm Mới Sản phẩm
          table(class="table table-hover table-sm" check-box-multi)
            thead
              tr
                th
                  input(type="checkbox" name="checkall")
                th stt
                th hinh anh
                th tieu de
                th vị trí
                th trang thai
                th hanh dong
            tbody         
                +renderCategoryIndex(category)
//-   nav
//-     ul(class="pagination")
//-       li(class="page-item")
//-       +objectPagination(objectPagination)